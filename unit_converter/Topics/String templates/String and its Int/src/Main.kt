fun main() {
    // write here
    val stringNumber = readln()
    println(
        "The obtained value is $stringNumber and its Int representation " +
                "after division on 2 is ${stringNumber.toInt() / 2}"
    )
}